<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.VangtiChai">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!--
                configChanges is intentionally OMITTED. The assignment wants us to show
                that state survives a real configuration change (rotation), which is
                exactly what onSaveInstanceState/onRestoreInstanceState are for. We let
                the system destroy/recreate the Activity on rotation and restore the
                entered amount there (see MainActivity.kt).
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
